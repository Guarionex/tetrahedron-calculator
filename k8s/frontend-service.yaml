apiVersion: v1
kind: Service
metadata:
  name: tetrahedron-frontend
spec:
  selector:
    app: tetrahedron-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
